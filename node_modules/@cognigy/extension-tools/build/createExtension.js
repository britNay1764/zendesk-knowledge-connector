"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createExtension = void 0;
const createExtension = (params) => {
    const createExtensionReturnValue = {
        nodes: params.nodes,
        knowledge: params.knowledge || [],
        connections: params.connections || [],
        appTemplates: params.appTemplates || [],
        options: params.options || {}
    };
    /**
     * New path which allows to use tools, but only if a feature-flag
     * is present.
     */
    if (process.env.FEATURE_ENABLE_TOOLS_IN_EXTENSIONS === "true") {
        return {
            ...createExtensionReturnValue,
            tools: params.tools || [],
        };
    }
    return createExtensionReturnValue;
};
exports.createExtension = createExtension;
