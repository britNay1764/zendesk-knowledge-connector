"use strict";
/**
 * This file only contains an early prototype of the Tool Descriptor
 * concept.
 *
 * The goal would be to unify the typings as well as the logic as much
 * as possible with NodeDescriptors and their concept. For the prototype
 * the idea was to be as independent as possible.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.createToolDescriptor = void 0;
// todo: use "formFieldTypes" or "nodeFieldTypes" or at least a filtered variant of them!
const toolParameterTypes = [
    "text",
    "select",
    "connection",
    "json",
    "keyValuePair" // todo: check why this is missing in the extension framework!
];
const createToolDescriptor = (params) => {
    const { type, defaultLabel, defaultDescription, defaultParameters, function: toolFunction, } = params;
    return {
        type,
        defaultLabel: defaultLabel !== null && defaultLabel !== void 0 ? defaultLabel : type, // use type as fallback for default label
        defaultDescription: defaultDescription || '',
        defaultParameters: defaultParameters || [],
        function: toolFunction || null
    };
};
exports.createToolDescriptor = createToolDescriptor;
