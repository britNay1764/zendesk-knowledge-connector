export interface IAppTemplate {
    type: string;
    label: string;
    /**
     * Relative path pointing from the main 'module' of your extension
     * to a directory containing your xApp template, e.g.
     *
     * ./templates/flightSeatPicker
     */
    path: string;
}
